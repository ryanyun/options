namespace :db do
  desc 'Populate dummy contract and bids data' do
    chris = User.create(first_name: 'Chris'
    User.find_each do |user|
      5.times do
        Contract.create(
      end
    end
  end
end
